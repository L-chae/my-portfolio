export const STATIC_ANSWERS: Record<string, string> = {
  "가장 자신 있는 프로젝트의 아키텍처 설명해 줘": `
Rodia 프로젝트입니다. 핵심은 세 가지입니다.

- **단일 JSON 토큰** → 웹/모바일 디자인 값을 한 곳에서 관리, Zod로 빌드 단계 검증
- **Orval 자동화** → OpenAPI 스펙 기반으로 React Query 훅 자동 생성, 수동 타입 작성 제거
- **Feature API 계층** → 서버 응답을 Mapper로 변환, 서버 변경 시 UI 컴포넌트 수정 0건

"지금 필요한 것"과 "유지보수 비용"을 균형 잡은 설계입니다.
  `.trim(),

  "상태 관리를 다루는 본인만의 기준이 있나요?": `
세 가지 기준을 따릅니다.

- **책임 분리** — 서버 상태는 React Query, 클라이언트 UI 상태는 Zustand. 섞지 않습니다
- **예외 상황 먼저** — 네트워크 지연, 토큰 만료, 서버 에러 시 상태가 어떻게 전파되는지 먼저 설계
- **도구보다 목적** — 프로젝트 규모에 맞는 도구 선택. StoryLex에서 Redux 대신 Zustand를 선택한 이유도 이것

기능이 아니라 사용자 흐름이 끊기지 않는 것이 핵심입니다.
  `.trim(),

  "Orval을 도입한 구체적인 이유는?": `
API가 40개 이상으로 늘어나면서 수동 타입 작성의 한계가 왔습니다.

- 명세 변경 시 3~4곳 누락 → 런타임 에러 반복
- openapi-typescript는 훅 생성 미지원
- Orval은 OpenAPI 스펙 기반으로 React Query 훅까지 자동 생성

결과적으로 API 명세 변경 시 타입이 자동 동기화되어 컴파일 타임에 100% 감지 가능해졌습니다.
  `.trim(),

  "401 에러 큐(Queue) 로직 설명해줘": `
토큰 만료 시 여러 API가 동시에 401을 반환하는 문제가 있었습니다.

- 각 요청이 개별로 Refresh를 시도 → Refresh API 중복 호출 → 상태 불일치
- 해결: 첫 401 발생 시 Refresh 진행 중 플래그 설정, 나머지 요청은 Queue 대기
- Refresh 완료 후 Queue의 요청들을 새 토큰으로 일괄 재시도

핵심은 "토큰 재발급은 반드시 한 번만 실행"을 보장하는 것입니다.
  `.trim(),

  "React Query와 Zustand 분리 이유": `
두 도구의 목적이 다릅니다.

- **React Query** — 서버 데이터 캐싱, 재요청, 동기화 담당
- **Zustand** — 학습 옵션, 모달 상태 등 클라이언트 UI 상태 담당

섞으면 디버깅 시 어느 쪽 문제인지 추적이 어렵습니다. 분리 기준은 단순합니다. "서버에서 오는가, 아닌가"입니다.
  `.trim(),

  "이전 프로젝트에서 겪은 가장 큰 난관은?": `
Rodia에서 관리자와 사용자 서비스를 하나의 디자인 시스템으로 공통화하려다 실패했습니다.

- 관리자는 데이터 중심, 사용자는 경험 중심 — 목적 자체가 달랐음
- 억지로 맞추다 보니 분기 로직만 늘어나 유지보수성 오히려 악화

"무조건 공통화"보다 "도메인 목적에 맞는 격리"가 더 가치 있다는 걸 배웠습니다.
  `.trim(),

  "협업 시 본인만의 강점은?": `
두 가지입니다.

- **모르는 것을 아는 척하지 않음** — 막혔을 때 혼자 안고 있지 않고 현재 상황을 투명하게 공유
- **상대방 언어로 소통** — 직군별 도메인 차이를 인정하고 전문 용어 대신 직관적 비유 활용

협업에서 정답을 내는 것보다 상황을 투명하게 공유하는 게 더 중요하다고 생각합니다.
  `.trim(),
};