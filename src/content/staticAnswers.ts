export const STATIC_ANSWERS: Record<string, string> = {
  "가장 자신 있는 프로젝트의 아키텍처 설명해 줘": `
> 핵심 아키텍처는 **웹/모바일 단일화**와 **API 계층 분리**입니다. (Rodia 프로젝트)

* **JSON 디자인 토큰**
  플랫폼 종속성 제거 및 Zod를 통한 빌드 단계 에러 검증
* **Orval 자동화**
  OpenAPI 스펙 기반 React Query 훅 자동 생성으로 수동 타입 작성 제거
* **Feature API (Mapper)**
  서버 응답을 중간 계층에서 변환하여 서버 구조 변경 시 UI 수정 비용 0건 달성

"지금 당장 필요한 기능"과 "미래의 유지보수 비용"의 타협점을 맞춘 설계입니다.
  `.trim(),

  "상태 관리를 다루는 본인만의 기준이 있나요?": `
> **'상태의 성격'**과 **'예외 흐름 통제'**를 최우선으로 둡니다.

1. **책임 분리**
   서버 상태(React Query)와 UI 상태(Zustand)를 격리하여 디버깅 비용 감소
2. **예외 우선 설계**
   네트워크 지연, 토큰 만료 시 상태가 어떻게 전파되는지 먼저 구축
3. **적정 도구 선택**
   글로벌 상태 공유가 적은 곳엔 무거운 Redux 대신 가벼운 Zustand 도입

단순 기능 구현보다 예외 상황에서도 사용자 흐름이 끊기지 않는 것이 핵심입니다.
  `.trim(),

  "오버엔지니어링으로 실패했던 경험이 있나요?": `
> Rodia에서 관리자 서비스와 사용자 서비스를 과하게 공통화하려다 유지보수성이 나빠진 경험이 있습니다.

* **문제**: 데이터 중심 화면과 경험 중심 화면을 같은 규칙으로 묶으려 함
* **결과**: 분기 로직이 늘고, 작은 변경도 여러 조건을 확인해야 하는 구조가 됨
* **교훈**: 공통화보다 도메인의 목적에 맞는 격리가 더 가치 있을 때가 있음

이후에는 "미래 확장성"보다 "현재 팀 규모와 변경 빈도"를 먼저 보고 설계 범위를 정합니다.
  `.trim(),

  "HiveLab 경험이 프론트엔드에 어떻게 연결되나요?": `
> AI 학습 데이터 검수 경험은 프론트엔드에서 **데이터 신뢰도와 예외 흐름**을 먼저 보는 습관으로 이어졌습니다.

검수 업무에서는 작은 기준 차이도 결과 품질을 흔들 수 있었습니다. 그래서 프론트엔드에서도 서버 데이터가 항상 완벽하다고 가정하지 않고, 누락값·지연·불일치가 들어왔을 때 화면이 어떻게 반응해야 하는지를 먼저 설계합니다.

즉, 단순히 UI를 그리는 것보다 데이터 상태가 흔들릴 때 사용자 경험을 지키는 쪽에 강점이 있습니다.
  `.trim(),

  "HiveLab에서 어떤 일을 했나요?": `
> HiveLab에서는 AI 학습 데이터 라벨링과 검수 업무를 담당했습니다.

AI 모델이 데이터를 잘 이해할 수 있도록 일정한 기준에 맞춰 데이터를 분류하고, 결과의 품질을 확인하는 일이었습니다. 단순 반복처럼 보일 수 있지만 작은 오류 하나가 결과 품질에 영향을 줄 수 있어서 꼼꼼함과 기준의 일관성이 중요했습니다.

이 경험이 지금은 프론트엔드에서 데이터 신뢰도와 예외 흐름을 먼저 확인하는 습관으로 이어졌습니다.
  `.trim(),

  "데이터 품질을 왜 중요하게 보나요?": `
> 데이터 품질은 화면의 안정성과 사용자 신뢰를 직접 결정하기 때문입니다.

* **불완전한 데이터**는 런타임 에러나 빈 화면으로 이어질 수 있음
* **일관성 없는 데이터**는 조건 분기와 예외 처리를 계속 늘림
* **검증 없는 데이터**는 문제가 배포 후에야 드러남

그래서 Zod 검증, mapper 계층, fallback 처리처럼 데이터가 UI에 닿기 전에 한 번 정리되는 구조를 선호합니다.
  `.trim(),

  "데이터 품질과 일관성이 왜 중요했나요?": `
> AI 학습 데이터는 양이 많아도 기준 없이 섞이면 모델이 제대로 학습하기 어렵습니다.

HiveLab 경험에서 가장 크게 배운 점은 데이터의 양보다 품질과 일관성이 중요하다는 것이었습니다. 같은 유형의 데이터가 서로 다른 기준으로 처리되면 결과가 흔들리고, 작은 오류도 전체 품질에 영향을 줄 수 있습니다.

그래서 지금도 프론트엔드 개발에서 서버 데이터가 올바른 기준으로 전달되는지, 비어 있거나 흔들릴 때 화면이 안전하게 반응하는지를 먼저 봅니다.
  `.trim(),

  "반복 비용을 줄인 경험이 있나요?": `
> Rodia에서 디자인 토큰과 API 타입 생성을 자동화해 반복 비용을 줄였습니다.

JSON 토큰을 단일 소스로 두고, 빌드 과정에서 웹/모바일용 토큰 파일을 생성했습니다. API는 OpenAPI 스펙 기반 Orval을 도입해 타입과 React Query 훅을 자동 생성했습니다.

핵심은 사람이 매번 기억해서 처리해야 하는 일을 시스템이 검증하고 생성하게 만든 것입니다.
  `.trim(),

  "반복 작업의 휴먼 에러를 어떻게 줄였나요?": `
> 반복 작업에서 실수가 생기기 쉬운 지점을 먼저 찾고, 작업 기준과 순서를 정리해 줄였습니다.

HiveLab 업무에는 반복적인 검수 작업이 많았기 때문에 휴먼 에러가 발생하기 쉬웠습니다. 그래서 단축 도구나 자동화 방법을 찾아 적용했고, 예외 기준과 작업 순서를 문서화해 같은 판단을 반복할 때 기준이 흔들리지 않도록 했습니다.

이 경험 때문에 지금도 반복되는 문제를 그냥 처리하지 않고, 어떻게 시스템이나 구조로 줄일 수 있을지를 먼저 생각합니다.
  `.trim(),

  "시나리오 검증가라는 말은 무슨 뜻인가요?": `
> 기능이 정상 동작하는지만 보는 것이 아니라, 실패 흐름까지 먼저 검증하는 개발자라는 뜻입니다.

사용자는 100번의 정상 동작보다 1번의 실패 경험을 더 오래 기억합니다. 그래서 네트워크 지연, 인증 만료, null 데이터, 중복 요청 같은 상황을 미리 가정하고 화면과 상태가 안전하게 반응하도록 설계합니다.

저는 이 관점을 프론트엔드의 중요한 품질 기준으로 보고 있습니다.
  `.trim(),

  "예외 상황을 왜 먼저 고민하나요?": `
> 예외 상황은 사용자 경험이 가장 쉽게 무너지는 지점이기 때문입니다.

정상 흐름만 보고 구현하면 장애, 지연, 토큰 만료, 데이터 누락 같은 상황에서 화면이 멈추거나 사용자가 이유를 알 수 없는 실패를 겪게 됩니다.

그래서 구현 초기에 "실패하면 무엇을 보여줄지", "요청이 중복되면 어떻게 막을지", "데이터가 비어 있으면 어떻게 보정할지"를 먼저 정합니다.
  `.trim(),

  "오버엔지니어링을 줄이는 기준은?": `
> 현재 문제의 크기, 변경 빈도, 팀이 감당할 수 있는 복잡도를 기준으로 판단합니다.

* **지금 해결할 문제인가**
* **반복 변경이 실제로 예상되는가**
* **추가 계층이 디버깅 비용을 줄이는가**
* **팀원이 구조를 쉽게 이해할 수 있는가**

이 기준을 통과하지 못하면 확장성보다 단순한 구현을 선택합니다.
  `.trim(),

  "Rodia 디자인 시스템 어때?": `
> Rodia 디자인 시스템의 핵심은 **디자인 자산을 관리하는 도구**가 아니라 **변경 프로세스를 자동화한 구조**입니다.

* **JSON 토큰**을 단일 진실 공급원으로 두고 웹/모바일 파일을 자동 생성
* **Zod 검증**으로 토큰 오류를 빌드 단계에서 차단
* **모노레포**로 웹과 모바일 변경을 하나의 PR에서 동기화

결과적으로 UI 변경 공수를 줄이고, 플랫폼 간 스타일 불일치와 휴먼 에러를 줄이는 데 초점을 둔 프로젝트입니다.
  `.trim(),

  "StoryLex 401 에러 해결 과정 알려줘": `
> StoryLex에서는 여러 API가 동시에 401을 반환할 때 토큰 재발급이 중복 실행되는 문제가 있었습니다.

해결 방식은 Promise Queue였습니다. 첫 401 요청만 refresh를 수행하고, 이후 요청들은 queue에 대기시킵니다. refresh가 끝나면 대기 중인 요청들을 새 토큰으로 다시 실행합니다.

이 구조로 인증 만료 상황에서도 사용자가 화면 멈춤이나 로그아웃 튐을 덜 겪도록 만들었습니다.
  `.trim(),

  "Orval을 도입한 구체적인 이유는?": `
> 40개 이상의 API 명세 변경 시 발생하는 **런타임 에러를 컴파일 타임에 100% 차단**하기 위함입니다.

* **수동 타입 작성**: 명세 변경 시 누락 발생 → 런타임 장애
* **openapi-typescript**: 타입만 생성되고 훅(Hook) 자동 생성 미지원
* **Orval**: OpenAPI 스펙 기반으로 타입과 React Query 훅을 동시 생성

결과적으로 백엔드 명세 변경 시 프론트엔드 타입이 자동 동기화되어 휴먼 에러를 제거했습니다.
  `.trim(),

  "Rodia 아키텍처를 더 자세히 설명해 줘": `
> Rodia는 크게 **토큰 자동화**, **모노레포**, **API 계층 분리**로 구성했습니다.

디자인 토큰은 JSON을 기준으로 웹/모바일 산출물을 생성하고, Zod로 스키마를 검증했습니다. API는 Orval로 generated 코드를 만들되 UI가 직접 쓰지 않고 Feature API와 mapper 계층을 통해 소비하게 했습니다.

이렇게 자동 생성 코드와 실제 화면 사이에 완충 계층을 둬서 서버 변경의 영향 범위를 줄였습니다.
  `.trim(),

  "모노레포를 도입한 이유는?": `
> 웹과 모바일의 디자인 토큰을 같은 변경 단위로 관리하기 위해서입니다.

저장소가 분리되어 있으면 토큰 변경 후 양쪽 프로젝트에 따로 반영하고 배포 타이밍도 맞춰야 합니다. Rodia에서는 pnpm workspace 기반 모노레포로 묶어 한 번의 커밋과 PR에서 웹/모바일 변경을 같이 처리했습니다.

프로젝트 규모상 Turborepo까지는 과하다고 판단했고, pnpm workspace만으로 충분한 수준을 선택했습니다.
  `.trim(),

  "Orval과 Feature API를 왜 분리했나요?": `
> 자동 생성 코드를 UI가 직접 의존하면 서버 변경이 화면 전체로 번지기 때문입니다.

Orval은 OpenAPI 명세에서 타입과 React Query 훅을 생성하는 역할에 집중시켰고, Feature API는 서버 응답을 화면에 맞는 형태로 변환하는 역할을 맡겼습니다.

이 덕분에 서버 필드명이나 null 처리 방식이 바뀌어도 mapper 계층만 수정하면 되고, UI 컴포넌트의 변경 범위를 줄일 수 있었습니다.
  `.trim(),

  "401 에러 큐(Queue) 로직 설명해줘": `
> 여러 API 동시 호출 시 발생하는 **Refresh 중복 발생 및 상태 불일치**를 해결한 로직입니다.

* **원인**: 401 에러 발생 시 각 요청이 독립적으로 Refresh API 중복 호출
* **해결**: 최초 401 감지 시 '진행 플래그'를 세우고, 후속 API 요청은 **Promise Queue**에 대기
* **결과**: Refresh 완료 후 Queue 내부 요청들을 새로운 토큰으로 일괄 재실행

"토큰 재발급은 반드시 한 번만 실행되어야 한다"는 동시성 문제를 통제한 사례입니다.
  `.trim(),

  "React Query와 Zustand 분리 이유": `
> 상태 이상 발생 시 원인을 빠르게 파악하기 위해 **'데이터의 출처'**를 기준으로 나눴습니다.

* **React Query (서버 상태)**: 서버 데이터 캐싱, 동기화, 재요청 전담
* **Zustand (클라이언트 상태)**: 모달 상태, 로컬 학습 옵션 등 UI 상태 전담

이 둘을 섞게 되면 디버깅 시 클라이언트 로직 문제인지, 서버 응답 문제인지 추적하기 어려워집니다.
  `.trim(),

  "StoryLex에서 예외 처리를 어떻게 설계했나요?": `
> StoryLex는 학습 흐름이 끊기지 않는 것을 목표로 예외 처리를 설계했습니다.

가장 중요한 케이스는 인증 만료였습니다. 대시보드 진입 시 여러 API가 동시에 호출되기 때문에, 401이 여러 개 발생해도 refresh는 한 번만 실행되도록 queue 구조를 만들었습니다.

또 서버 상태는 React Query, 화면 상태는 Zustand로 분리해 오류 원인을 더 빨리 추적할 수 있게 했습니다.
  `.trim(),

  "포트폴리오 AI는 RAG 없이 어떻게 동작하나요?": `
> 데이터 규모가 작기 때문에 벡터 DB 기반 RAG 대신 정적 컨텍스트 주입 방식을 선택했습니다.

포트폴리오 지식을 주제별 파일로 나누고, 질문이 들어오면 searchKnowledge가 관련 섹션을 찾아 프롬프트 컨텍스트로 넣습니다. 준비된 추천 질문은 STATIC_ANSWERS에서 바로 처리해 API 호출 없이 답변합니다.

핵심은 모델보다 데이터 구조입니다. Problem, Decision, Result 형태로 정리해 답변이 일관되게 나오도록 했습니다.
  `.trim(),

  "추천 질문 UX는 어떻게 개선했나요?": `
> 사용자의 위치와 마지막 답변 맥락에 따라 추천 질문이 바뀌도록 개선했습니다.

처음에는 현재 섹션에 맞는 질문을 보여줍니다. 예를 들어 Projects 섹션에서는 Rodia, StoryLex, Portfolio AI 질문이 먼저 뜹니다. 사용자가 질문을 선택하면 검색된 지식 id를 activeContextKey에 저장하고, 답변 이후에는 해당 주제의 후속 질문으로 교체됩니다.

이렇게 단순한 버튼 목록이 아니라 대화를 이어주는 탐색 장치로 만들었습니다.
  `.trim(),

  "AI 챗봇의 환각 방지는 어떻게 했나요?": `
> 답변 범위를 포트폴리오 지식 데이터로 제한하고, 근거 없는 내용은 말하지 않도록 시스템 프롬프트를 설계했습니다.

또 질문과 관련된 지식 섹션만 컨텍스트로 넣어 모델이 참고할 범위를 좁혔습니다. 자주 쓰는 추천 질문은 정적 답변으로 처리해 불필요한 API 호출과 답변 변동성도 줄였습니다.

즉, 모델 성능에만 기대기보다 데이터 구조와 답변 경로를 함께 통제했습니다.
  `.trim(),

  "이전 프로젝트에서 겪은 가장 큰 난관은?": `
> Rodia 프로젝트에서의 **도메인 무시 및 무리한 공통화 실패** 경험입니다.

* **상황**: 데이터 중심의 '관리자' 서비스와 경험 중심의 '사용자' 서비스를 단일 디자인 시스템으로 통합 시도
* **결과**: 두 서비스의 목적이 달라 억지로 맞추기 위한 분기 처리(If/Else)가 급증하여 유지보수성 악화
* **교훈**: 맹목적인 공통화보다 도메인 목적에 맞는 **적절한 격리(Isolation)**가 아키텍처 관점에서 더 가치 있음을 배웠습니다.
  `.trim(),

  "협업 시 본인만의 강점은?": `
> 정답을 강요하기보다 **투명한 상황 공유**를 지향합니다.

1. **모르는 것을 아는 척하지 않음**
   작업이 막혔을 때 혼자 안고 있지 않고, 현재 병목 상태를 조기 공유
2. **상대방 언어로 소통**
   기획자, 디자이너 등 타 직군 소통 시 개발 전문 용어를 배제하고 직관적 비유 활용

협업 시스템에서는 개인의 퍼포먼스보다 진행 상황의 투명한 동기화가 팀 효율에 더 큰 영향을 미친다고 생각합니다.
  `.trim(),

  "어떤 개발 환경에서 가장 잘 일하나요?": `
> 자유롭게 의견을 나누고 코드 리뷰로 서로 배울 수 있는 팀에서 가장 잘 일합니다.

특히 복잡한 비동기 흐름을 정리하거나, 파편화된 구조를 안정적인 구조로 개선하는 작업에 강점이 있습니다. 완전히 새로운 기능을 밑바닥부터 설계하는 일도 좋아합니다.

다만 기술적 완벽함보다 현재 팀의 상황과 유지보수 비용을 함께 보는 환경일수록 더 좋은 결과를 냅니다.
  `.trim(),

  "AI 도구를 어떻게 검증해서 사용하나요?": `
> AI 결과를 바로 믿기보다 여러 모델과 기준으로 교차 검증합니다.

설계 방향, 상태 관리 방식, 컴포넌트 분리, 예외 처리 누락 여부를 비교합니다. 코드가 동작하더라도 장기적으로 수정하기 어렵거나 책임이 섞여 있으면 다시 분리합니다.

AI는 생산성을 높이는 도구이고, 최종 판단과 책임은 개발자가 가져야 한다고 생각합니다.
  `.trim(),

  "AI 시대에 개발자의 핵심 역량은?": `
> 코딩 속도보다 문제 정의, 기술 판단, 결과 검증 능력이 더 중요해진다고 봅니다.

AI가 기본 구현을 빠르게 도와줄 수는 있지만, 어떤 문제를 풀어야 하는지, 어떤 구조가 팀에 적합한지, 예외 상황을 어떻게 방어할지는 사람이 결정해야 합니다.

그래서 저는 절약된 시간을 아키텍처 설계와 의사결정 품질을 높이는 데 쓰는 것이 중요하다고 봅니다.
  `.trim(),

  "Codex나 Claude를 실무에 어떻게 활용했나요?": `
> 반복적인 UI 구현, 보일러플레이트 생성, 구조 후보 비교에 활용했습니다.

Rodia 이후부터 IDE 내장 에이전트와 Claude CLI 같은 도구를 더 적극적으로 썼고, 저는 생성된 코드를 검토하면서 책임 분리와 예외 처리 기준을 맞췄습니다.

즉, AI에게 구현 속도를 맡기되 설계 판단과 품질 검증은 직접 가져가는 방식으로 사용했습니다.
  `.trim(),

  "문제를 재현하는 기준은?": `
> 같은 조건에서 다시 발생시킬 수 있어야 원인을 검증할 수 있다고 봅니다.

먼저 입력값, 사용자 행동, 네트워크 상태, 데이터 상태를 분리해서 확인합니다. 재현 조건이 정리되면 팀원에게도 명확히 공유할 수 있고, 수정 후 같은 조건으로 회귀 여부를 확인할 수 있습니다.

재현 없는 수정은 운 좋게 증상만 가리는 경우가 많아서 가장 경계합니다.
  `.trim(),

  "버그 수정 전에 무엇을 먼저 확인하나요?": `
> 현상보다 영향 범위와 재현 조건을 먼저 확인합니다.

문제가 특정 화면만의 문제인지, 공통 API 계층이나 상태 관리 구조의 문제인지부터 나눕니다. 그 다음 정적 분석, 타입 에러, 네트워크 응답, 상태 변화 순서처럼 확인 가능한 단서부터 좁혀갑니다.

이 과정을 거쳐야 수정이 다른 기능을 깨지 않는지 판단할 수 있습니다.
  `.trim(),

  "예외 상황을 어떻게 통제하나요?": `
> 예외를 화면 가까이에서 임시 처리하기보다, 데이터와 상태가 흐르는 경계에서 정리하려고 합니다.

서버 응답은 mapper에서 fallback을 적용하고, 인증 만료 같은 비동기 예외는 queue나 전역 API 계층에서 처리합니다. 화면 컴포넌트는 가능한 한 이미 정리된 상태를 받도록 만듭니다.

이렇게 해야 예외 처리가 중복되지 않고, 문제가 생겼을 때 원인을 찾기 쉬워집니다.
  `.trim(),
};
